apiVersion: v1
kind: PersistentVolume

metadata:
  name: mongo-pv-stateful

spec:
  capacity:
    storage: 2Gi

  accessModes:
    - ReadWriteOnce

  persistentVolumeReclaimPolicy: Retain

  storageClassName: ""

  hostPath:
    path: /data/mongo-stateful
